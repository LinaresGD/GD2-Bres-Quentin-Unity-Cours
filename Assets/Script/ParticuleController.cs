using UnityEngine;

public class ParticuleControlller : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        Destroy(gameObject,t:5f);
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
